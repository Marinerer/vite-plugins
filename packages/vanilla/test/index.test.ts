// Initial test file for vanilla plugin
// We will add actual tests here later
import { describe, it, expect } from 'vitest';

describe('vanilla plugin', () => {
  it('should have tests', () => {
    expect(true).toBe(true);
  });
});
