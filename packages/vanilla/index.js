const PLUGIN_NAME = "vite-plugin-vanilla"

/**
 * vanilla plugin
 * @param {string[]} pagesPatterns pages patterns
 * @param {object} options options
 * @returns 
 */
export default function createPlugin(pagesPatterns, options) {
  return {
    name: "vite-plugin-vanilla",
  }
}
