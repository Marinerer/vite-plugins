// Initial test file for page-html plugin
// We will add actual tests here later
import { describe, it, expect } from 'vitest';

describe('page-html plugin', () => {
  it('should have tests', () => {
    expect(true).toBe(true);
  });
});
